describe('Test', function() {
  describe('of tests', function () {
    it('just dummy test', function () {
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    });
  });
});